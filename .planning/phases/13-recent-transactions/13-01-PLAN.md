---
phase: 13-recent-transactions
plan: 01
type: execute
wave: 1
depends_on: [12-02]
files_modified:
  - apps/api/src/routes/transactions.ts
  - apps/api/src/routes/transactions.test.ts
  - apps/api/src/index.ts
  - apps/web/src/hooks/useTransactions.ts
  - apps/web/src/components/DashboardInterface/index.tsx
autonomous: true
requirements: [SPND-01]
---

<objective>
Build the recent transactions feature: a GET /api/transactions endpoint that reads from the database, a useTransactions React hook, and wire DashboardInterface to show real transaction history instead of mock data.

Purpose: Dashboard currently shows hardcoded MOCK_TRANSACTIONS. This plan replaces them with real data from the transactions table.

Output: GET /api/transactions endpoint, useTransactions hook, DashboardInterface wired to live data.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create GET /api/transactions route with tests and mount it</name>
  <files>
    apps/api/src/routes/transactions.ts,
    apps/api/src/routes/transactions.test.ts,
    apps/api/src/index.ts
  </files>
  <action>
    Create `apps/api/src/routes/transactions.ts`:
    - Export `transactionsRoute = new Hono()`
    - GET / handler accepts `?userId=` query param
    - Validate userId is present (400 MISSING_USER_ID if not)
    - Query transactions table: `db.select().from(transactions).where(eq(transactions.senderUserId, userId)).orderBy(desc(transactions.createdAt)).limit(20)`
    - Return `{ transactions: [...] }` with fields: id, amountUsd, recipientWallet, status, category, source, createdAt, txHash
    - 500 error with FETCH_FAILED on DB error

    Create `apps/api/src/routes/transactions.test.ts`:
    - Mock @genie/db
    - Test 1: GET /?userId=valid-uuid returns 200 with transactions array
    - Test 2: GET / without userId returns 400 MISSING_USER_ID
    - Test 3: DB error returns 500 FETCH_FAILED

    Mount in `apps/api/src/index.ts`:
    - Add `import { transactionsRoute } from './routes/transactions'`
    - Add `app.route('/api/transactions', transactionsRoute)` after balanceRoute
  </action>
  <verify>
    <automated>cd /Users/kerem/genie && pnpm --filter @genie/api test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>GET /api/transactions returns real transaction data from DB. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create useTransactions hook and wire DashboardInterface</name>
  <files>
    apps/web/src/hooks/useTransactions.ts,
    apps/web/src/components/DashboardInterface/index.tsx
  </files>
  <action>
    Create `apps/web/src/hooks/useTransactions.ts`:
    - Same pattern as useBalance.ts
    - Accept `userId: string`
    - Fetch `${API_URL}/api/transactions?userId=${userId}`
    - Return `{ transactions, loading, error }`
    - Each transaction has: id, amountUsd, recipientWallet, status, category, source, createdAt, txHash

    Update `apps/web/src/components/DashboardInterface/index.tsx`:
    - Import useTransactions
    - Call `useTransactions(session?.user?.id ?? '')`
    - Replace MOCK_TRANSACTIONS rendering with live data
    - Format: label = `Sent to ${tx.recipientWallet.slice(0, 6)}...${tx.recipientWallet.slice(-4)}`, amount = `-$${parseFloat(tx.amountUsd).toFixed(2)}`, time = relative date (use simple formatting)
    - Show loading skeleton while loading
    - Show "No transactions yet" when empty
    - Remove MOCK_TRANSACTIONS constant
  </action>
  <done>DashboardInterface shows real transactions from API. Loading/empty states handled.</done>
</task>

</tasks>

<success_criteria>
- GET /api/transactions?userId= returns real data from transactions table
- DashboardInterface shows real transaction history instead of mock data
- All API tests pass
</success_criteria>
